import 'package:flutter/material.dart';

class AddShortScreen extends StatelessWidget {
  const AddShortScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Short Video')),
      body: const Center(child: Text('Camera/Upload UI Placeholder')),
    );
  }
}
